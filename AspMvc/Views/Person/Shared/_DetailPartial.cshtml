@model Person

@if (@Model == null)
{
    <p><strong>The person doesn't exist!</strong></p>
}
else
{
    <h3>(ID: @Model.PersonId) @Model.LastName, @Model.FirstName</h3>

    <p>City: @Model.City</p>

    <p>Phone: @Model.Phone</p>
}
